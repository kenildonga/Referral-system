export enum AdminRole {
  SUPER_ADMIN = 'superAdmin',
  ADMIN = 'admin',
}

export enum SubmissionUserType {
  AGENT = 'agent',
  USER = 'user',
}

export enum UserStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum BankHolderType {
  USER = 'user',
  AGENT = 'agent',
}

export enum OtpPurpose {
  REGISTRATION = 'registration',
  PASSWORD_RESET = 'password_reset',
}
